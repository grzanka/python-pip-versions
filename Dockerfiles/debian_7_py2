FROM debian:7

# Add supervisor cfg, start it
ADD discover.sh /discover.sh

RUN /discover.sh py2

CMD (python -V >> ver.log 2>&1 || python3 -V >> ver.log 2>&1) && (pip -V >> ver.log 2>&1 || pip3 -V >> ver.log 2>&1) && cat ver.log
